<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saathi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".NewDoctor" />
        <activity android:name=".YourPatients" />
        <activity android:name=".YourDoctors" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".DDashboard" />
        <activity android:name=".AddReading" />
        <activity android:name=".NewReading" />
        <activity android:name=".PDashboard" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>